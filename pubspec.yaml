name: work_test
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # States
  flutter_bloc: ^8.1.5
  equatable: ^2.0.5

  # Requests
  dio: ^5.3.4
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
   - assets/bg_card.png
   - assets/logo_pokemon.png
   - assets/logo_rick_and_mortie.png

